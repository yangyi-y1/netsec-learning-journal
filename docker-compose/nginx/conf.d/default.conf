server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name cssman.top www.cssman.top _;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ /\. {
        deny all;
    }
}
